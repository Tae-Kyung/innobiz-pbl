# AI특화공동훈련센터 PBL 컨설팅 보고서 자동생성 시스템

## 프로젝트 개요
- **목적**: PBL 사업 컨설턴트가 기업 방문/면담 결과를 입력하면 서식3~5 보고서를 자동 생성
- **사용자**: PBL 컨설턴트 (1인 사용)
- **기술 스택**: Next.js 15 (App Router), TypeScript, Tailwind CSS, Supabase (DB + Auth), Vercel 배포
- **AI**: Claude API (claude-sonnet-4-6) - 보고서 생성용

## 서식 체계 (6종)
| 서식 | 이름 | 시스템 역할 |
|---|---|---|
| 서식1 | AX 수준진단표 | 기업이 작성 → **점수 입력/자동계산** |
| 서식2 | AX 세부진단표 | **인터뷰 결과 입력 + AI 분석** |
| 서식3 | 컨설팅 수행일지 | **회차별 입력 → AI 생성** |
| 서식4 | 컨설팅 결과보고서 | 서식1~3 기반 **AI 종합 생성** |
| 서식5 | 훈련 운영계획서 | 서식4 기반 **AI 생성** |
| 서식6 | 훈련 결과보고서 | 훈련 후 수동 입력 (향후) |

## 핵심 워크플로우
```
기업 등록 → 서식1 점수 입력 → 서식2 인터뷰 입력(영역별)
→ 서식3 수행일지 생성(8회차) → 서식4 결과보고서 생성
→ 서식5 훈련계획서 생성
```

## 업무 맥락 (guide.md 기반)
- 발주처: (사)이노비즈협회 충북일자리지원센터
- 서식1: 기업이 직접 작성 → 협회가 정리해서 컨설턴트에게 공유 (시스템에는 점수만 입력)
- 서식2: 구글 폼으로 기업에 사전 접수 → 접수 내용을 인터뷰 시 활용
- **작성 필요 서식**: 서식3(8회 방문, 예산 240만원), 서식4(종합+과정 도출), 서식5(예산 100만원)
- 서식6: 재직자 훈련 진행 시에만 (향후 대응)
- 작성 순서: 서식1·2(접수) → 서식3(현장 8회) → 서식4(종합·과정 도출) → 서식5(과정 세분화)

## 코딩 컨벤션
- 한국어 UI, 영어 코드
- 파일 구조: feature-based (app/companies, app/reports 등)
- DB: Supabase with Row Level Security
- 환경변수: `.env.local` (SUPABASE_URL, SUPABASE_ANON_KEY, ANTHROPIC_API_KEY)

## 설계 문서
- `docs/PRD.md` - 제품 요구사항
- `docs/Architecture.md` - 아키텍처 (DB 스키마, API, 폴더 구조)
- `docs/Tasks.md` - 구현 태스크 목록
